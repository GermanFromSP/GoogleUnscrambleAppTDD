<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/loadContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical">

    <ru.german.android.expertcourseunscrambleword.views.error.ErrorView
        android:id="@+id/errorTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/no_internet"
        android:gravity="center"
        android:padding="8dp" />

    <ru.german.android.expertcourseunscrambleword.views.retry.RetryButton
        android:id="@+id/retryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/purple"
        android:layout_margin="32dp"
        android:padding="16dp"
        android:text="@string/retry"
        android:textColor="@color/white" />

    <ru.german.android.expertcourseunscrambleword.views.progress.LoadView
        android:id="@+id/loadProgress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="32dp" />

</LinearLayout>